---
schema_version: 99
name: unsupported-schema
display_name: Unsupported Schema
description: A canonical that declares a schema version the builder does not support.
---

# Unsupported Schema

## Vision and Users

A structurally fine document whose declared schema_version the builder has never written. (Version 1 is deliberately NOT used here — 1 gets the gentler older-schema/import path; this fixture is the outright-unknown case.)
