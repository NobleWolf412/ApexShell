---
schema_version: 1
name: malformed-frontmatter
display_name: Malformed Frontmatter
description: The opening delimiter is never closed, so frontmatter cannot parse.

# Malformed Frontmatter

## Vision and Users

Body that the parser never reaches because the frontmatter block is unterminated.
